<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.DoTrack" parent="android:Theme.Material.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.DoTrack" parent="Base.Theme.DoTrack" />

    <!-- SplashScreen theme (#101) - displayed until first frame -->
    <style name="Theme.DoTrack.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_app_icon</item>
        <item name="postSplashScreenTheme">@style/Theme.DoTrack</item>
    </style>
</resources>